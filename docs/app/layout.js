export const metadata = {
    "title": "Gragle - Documentation",
}

export default function RootLayout({content}) {
    return (
        <html lang="en">
            <body>{content}</body>
        </html>
    );
}